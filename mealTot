<!DOCTYPE html>
<html>
  <body>

    <h1>JavaScript IPO</h1>

      <p>Input:</p>

      <input id="in">

      <button type="button" onclick="myFunction()">Submit</button>

      <p id="out"></p>

    <script>
    function myFunction() {
        var mealTot, granTot, taxTot;

        // Input
        mealTot = document.getElementById("in").value;tById("in").value;

        // Processing
        outVar = inVar;
        taxTot = parseInt("mealTot") * 0.06;
        granTot =(parseInt("mealTot") * 0.20) + taxTot;

        // Output
        document.getElementById("out").innerHTML = outVar;
    }
    </script>

  </body>
</html>
